import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'qodkvj',
  brand: {
    primaryColor: '#3182F6',
  },
  navigationBar: {
    withTitle: true,
  },
  permissions: [],
  webBundleDir: 'dist',
});