import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'qodkvk',
  brand: {
    primaryColor: '#3182F6',
  },
  navigationBar: {
    withTitle: true,
  },
  permissions: [],
  webBundleDir: 'dist',
});